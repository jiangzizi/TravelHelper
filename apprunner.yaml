version: 1.0
runtime: python3
build:
  commands:
    build:
      - yum install pkg-config python3-dev default-libmysqlclient-dev build-essential
      - pip install -r requirements.txt
run:
  runtime-version: 3.8.16
  command: sh startup.sh
  network:
    port: 8000